/**
 * This package contains models (not services).
 * The "dto" is used for incoming requests and outgoing responses.
 * The "entity" is used for DB models.
 * The "mapper" is supposed for mapping between entities and DTOs.
 */
package com.family_tasks.tracker.task.model;